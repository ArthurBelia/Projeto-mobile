// Este arquivo é ignorado quando expo-router está ativo (package.json: "main": "expo-router/entry").
// A navegação é gerenciada pelo expo-router via app/(tabs)/index.tsx.
// Mantido aqui apenas como referência — não edite.
export {};
